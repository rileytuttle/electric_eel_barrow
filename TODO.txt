STORY_0: first story would be to strip knowledge of the ds4 from the controller state. we want controller state to be just about the intent of the controller. the controller state may need a higher level controller intent class. the controller intent class keeps track of intent and ez_cart_main doesn't know anything about the controller state only the controller intent. then the controller state should be renamed to ds4_state
