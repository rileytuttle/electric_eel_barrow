echo "sudo -H -u <user> /home/<user>/electric_eel_barrow/ez_cart_main.py &" >> /etc/rc.local

user is the user where the electric eel barrow repo lives

may need to make a wrapper script that checks for exits codes and restarts
and I can use that for now to restart periodically if there is no controller connected

made a wrapper script for non zero exit codes

so instead add
    change the above to

sudo -H -u pi /home/pi/electric_eel_barrow/ez_cart_runner.sh
